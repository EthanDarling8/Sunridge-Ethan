@page
@model Sunridge.Pages.Admin.Lot.Upsert

<form method="post" enctype="multipart/form-data">
    <div class="container">
        <div class="row px-2 mx-2 backgroundWhite border">
            <div asp-validation-summary="ModelOnly" class="text-danger">

            </div>
            <div class="col-12 px-3 border-bottom-1 mt-3">
                <h2 class="text-primary">@(Model.LotObj.Lot.Id != 0 ? "Update" : "Create") Lot </h2>
            </div>
            <div class="col-8 pt-3">
                <!-- Lot Number -->
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="LotObj.Lot.LotNumber"></label>
                    </div>
                    <div class="col-9">
                        <input class="form-control" asp-for="LotObj.Lot.LotNumber" />
                        <span class="text-danger" asp-validation-for="LotObj.Lot.LotNumber"></span>
                    </div>
                </div>

                <!--  Tax Id -->
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="LotObj.Lot.TaxId"></label>
                    </div>
                    <div class="col-9">
                        <input class="form-control" asp-for="LotObj.Lot.TaxId" />
                        <span class="text-danger" asp-validation-for="LotObj.Lot.TaxId"></span>
                    </div>
                </div>

                <!-- Street Address -->
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="LotObj.Lot.Address"></label>
                    </div>
                    <div class="col-9">
                        <input class="form-control" asp-for="LotObj.Lot.Address" />
                        <span class="text-danger" asp-validation-for="LotObj.Lot.Address"></span>
                    </div>
                </div>

                <!-- Owners-->
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="LotObj.Lot_Owner.OwnerId">Owner(s)</label>
                    </div>
                    <div class="col-9">
                        @Html.DropDownListFor(m => m.LotObj.Lot_Owner.OwnerId,
                                                Model.LotObj.Owners,
                                                "- Please select an Owner(s) - ",
                                                new { @class = "form-control" })
                        <span class="text-danger" asp-validation-for="LotObj.Lot_Owner.OwnerId"></span>
                    </div>

                </div>

                <!-- Inventory Items -->
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="LotObj.Lot_Inventory.InventoryId">Inventory Item(s)</label>
                    </div>
                    <div class="col-9">
                        @Html.DropDownListFor(m => m.LotObj.Lot_Inventory.InventoryId,
                                                Model.LotObj.Inventories,
                                                "- Please select an Inventory Item(s) - ",
                                                new { @class = "form-control" })
                        <span class="text-danger" asp-validation-for="LotObj.Lot_Inventory.InventoryId"></span>
                    </div>

                </div>

                <!-- Buttons -->
                <div class="form-group row">
                    <div class="col-9 offset-3">
                        <div class="row">
                            <div class="col-6">
                                @if (Model.LotObj.Lot.Id != 0)
                                {
                                    <button type="submit" class="btn btn-primary form-control">Update</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-primary form-control" onclick="return ValidateInput()">Create</button>
                                }
                            </div>
                            <div class="col-6">
                                <a asp-page="./Index" class="btn btn-success form-control">Back to List</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <script>
        tinymce.init({
            selector: 'textarea',
            plugins: "lists",
            menubar: 'file edit format'
        });

        function ValidateInput() {
            if (document.getElementById("uploadBox").value == "") {
                swal('Error', 'Please Select an Image', 'error');
                return false;
            }
            return true;
        }
    </script>
}
